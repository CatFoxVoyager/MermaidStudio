---
phase: 21-upgrade-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mermaid/__tests__/structure-goldens.test.ts
  - src/lib/mermaid/core.ts
  - tests/goldens/README.md
autonomous: true
requirements:
  - PIPE-01
  - UPG-02
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "src/lib/mermaid/__tests__/structure-goldens.test.ts is committed and green while node_modules/mermaid is 11.17.2 — the v11 baseline is captured BEFORE any v12 code lands (PIPE-01 ordering constraint)"
    - "The suite pins the flowchart structural contract with exact expected counts recorded from the v11 run: 3 `.edgePaths path.flowchart-link`, `g.edgeLabels` children correlating 1:1 with edge count, every `.node` id matching the flowchart-{ID}-{N} pattern"
    - "The suite pins the sequence structural contract: messageLine0/messageLine1 paths present, every marker-end reference resolving to an existing marker id, marker id substrings recorded from observation"
    - "Every `.edgePaths path` marker-end reference resolves to an existing marker element, and the `.root` reorder groups (nodes / edgePaths-or-edges / edgeLabels) are asserted present"
    - "mermaid.initialize() inside doInit() in src/lib/mermaid/core.ts carries layout: 'dagre' and look: 'classic' alongside the existing explicit theme (UPG-02)"
    - "The golden suite and existing rendered tests pass WITH the pins applied, still on mermaid 11.17.2 — the pins are empirically proven harmless under v11 before the flip"
    - "The suite contains no whole-SVG snapshot mechanism and no coordinate/path-data assertions — structural assertions only"
  artifacts:
    - path: "src/lib/mermaid/__tests__/structure-goldens.test.ts"
      provides: "v11 structural golden baseline (PIPE-01); re-runs as the v12 regression gate in Plans 02/03"
    - path: "tests/goldens/README.md"
      provides: "capture provenance: date, installed mermaid version, procedure, observed-selector table"
    - path: "src/lib/mermaid/core.ts"
      provides: "doInit() with layout/look v11-default pins (UPG-02)"
  key_links:
    - from: "src/lib/mermaid/__tests__/structure-goldens.test.ts"
      to: "src/lib/mermaid/core.ts"
      via: "renderDiagram() drives real mermaid in jsdom (getBBox polyfill) and returns the raw post-sanitize SVG under test"
      pattern: "renderDiagram\\("
    - from: "src/lib/mermaid/__tests__/structure-goldens.test.ts"
      to: "src/utils/svgPostProcessing.ts"
      via: "golden selectors mirror the pipeline's coupled selectors (.edgePaths path, g.edgeLabels, flowchart-{ID}-{N} regex, marker ids, rect.background, .root children)"
      pattern: "edgePaths|edgeLabels|flowchart-|marker"
  prohibitions:
    - statement: "MUST NOT weaken, loosen, or delete recorded golden assertions to make the suite pass under a future mermaid version — a 0-match is a finding to surface (Phase 22 owns pipeline selector fixes), never an edited baseline"
      status: unresolved
      verification: null
      category: transparency
      requirement_id: PIPE-01
    - statement: "MUST NOT implement the layout/look pins anywhere that outranks per-diagram frontmatter config.layout — user layout choices made in the style panel must keep overriding the initialize() pins"
      status: unresolved
      verification: null
      category: values
      requirement_id: UPG-02
---

<objective>
Capture the v11 structural golden baseline and pin the v11 rendering defaults — while mermaid 11.17.2 is still installed.

Purpose: PIPE-01 demands the golden fixture suite be captured BEFORE the mermaid 12 flip, or the v11-vs-v12 regression signal this milestone depends on is destroyed. The `layout: 'dagre'` / `look: 'classic'` pins land in the same plan because they are no-ops under v11 — landing them here keeps every subsequent commit revert-safe and lets the goldens empirically prove that harmlessness.
Output: committed `structure-goldens.test.ts` (green on v11), `tests/goldens/README.md` provenance, pinned `doInit()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-upgrade-compatibility/21-CONTEXT.md
@.planning/phases/21-upgrade-compatibility/21-RESEARCH.md
@.planning/phases/21-upgrade-compatibility/21-VALIDATION.md

@src/lib/mermaid/core.ts
@src/utils/svgPostProcessing.ts
</context>

## Artifacts this phase produces

Symbols and files this PHASE (all plans) creates or modifies — used to exclude new symbols from drift verification:

- NEW `src/lib/mermaid/__tests__/structure-goldens.test.ts` — PIPE-01 golden suite (this plan)
- NEW `tests/goldens/README.md` — capture provenance (this plan)
- MODIFIED `src/lib/mermaid/core.ts` — `doInit()` gains `layout: 'dagre'` + `look: 'classic'` initialize keys (this plan); ELK import + `registerLayoutLoaders` call deleted (Plan 02)
- MODIFIED `package.json` + `pnpm-lock.yaml` — mermaid exact 12.0.0, `@mermaid-js/layout-elk` removed (Plan 02)
- MODIFIED `vite.config.ts` — manualChunks rework, ELK lazy boundary preserved (Plan 02)
- MODIFIED `.github/workflows/ci.yml` — Node matrix `['22.12', 24]` (Plan 03)
- MODIFIED `src/components/modals/diagram/ExportModal.tsx` — embed at mermaid@12.0.0 + regenerated SRI (Plan 03)
- NEW `scripts/verify-cdn-embed.html` — standalone embed execution fixture (Plan 03)
- Build output: separate `elk-*` lazy chunk in `dist/assets` (Plan 02)

## Flagged assumptions (unresolved edge-probe items — surfaced, not silently dropped)

| Requirement | Probe category | Why unresolved is acceptable here |
|---|---|---|
| UPG-02 | unclassified | No probe criterion; behavior is pinned empirically by this plan's golden re-run with pins applied on v11. |
| PIPE-01 | unclassified | No probe criterion; capture correctness is enforced structurally (precondition on Plan 02's flip task: v11 still installed). |

## Working-tree note (research Pitfall 9, resolved by live inspection 2026-09-12)

The prior-session pipeline work (ExportModal/PreviewPanel/svgPostProcessing + pipeline tests) already
landed as commit `33bd53b` and dependency bumps as `14a943b`. The remaining dirty paths —
`skills-lock.json` (modified) and `.agents/skills/code-review-and-quality/` (untracked) — are GSD
skill-manager noise unrelated to this phase. Never stage them; phase commits stage only the files
each task names.

<tasks>

<task type="tracer">
  <name>End-to-end v11 golden capture — flowchart structural contract</name>
  <precondition>node_modules/mermaid/package.json reports version 11.17.2 (read-only check: node -p "require('./node_modules/mermaid/package.json').version") — the baseline MUST be captured on v11</precondition>
  <files>src/lib/mermaid/__tests__/structure-goldens.test.ts, tests/goldens/README.md</files>
  <read_first>
    - src/lib/mermaid/core.ts — renderDiagram signature and return contract ({svg, error}); note it returns post-sanitizeMermaidSVG output, PRE-postProcessDiagramSvg
    - src/utils/svgPostProcessing.ts lines 330-360 (rect.background query at :355), 405-425 (.root children reorder, edgePaths/edges + edgeLabels class names), 424-470 (marker hunting: id.includes('arrowhead') || id.includes('head'); .edgePaths path marker-end sync), 505-530 (flowchart-{ID}-{N} node id regex at :526)
    - tests/vitest.setup.ts — the getBBox polyfill that makes real mermaid renders deterministic in jsdom
    - src/lib/mermaid/__tests__/subgraph-render.test.ts — the proven render-in-jsdom pattern: renderDiagram call, getBBox-error tolerance, 30s timeout
    - src/utils/__tests__/postProcessDiagramSvg.test.ts — existing fixture shapes (flowchart + linkStyle default)
    - .planning/phases/21-upgrade-compatibility/21-RESEARCH.md — Pattern 1 (golden fixture design rules)
  </read_first>
  <action>
    Step 0 — triage: run git status; confirm the only dirty paths are skills-lock.json and .agents/skills/code-review-and-quality/. Stage ONLY the files this task names in commits.

    Create src/lib/mermaid/__tests__/structure-goldens.test.ts:
    - Header comment stating provenance: captured against mermaid 11.17.2, date 2026-09-12, Phase 21 PIPE-01; the suite re-runs as the regression gate after the v12 flip; the recorded assertions encode the v11 contract.
    - Fixture FLOWCHART (exact source):
      flowchart TD
        A[Start] -->|yes| B{Check}
        B -->|ok| C[End]
        B -->|fix| A
      linkStyle default stroke:#333333,fill:#ffffff,fill-opacity:1
    - Render via renderDiagram(FLOWCHART, 'golden_flow'); tolerate a getBBox-only error (existing pattern: if error and the message does not contain getBBox, throw); expect svg non-empty.
    - Parse the returned svg string with DOMParser ('image/svg+xml') and assert, with every expected value recorded from the ACTUAL v11 run (never from memory):
      1. querySelectorAll('.edgePaths path.flowchart-link').length === 3 (exact — derivable from the fixture's 3 edges)
      2. the g.edgeLabels element exists and its direct child count equals the edge path count (1:1 correlation invariant; record the observed child element name in the README)
      3. querySelectorAll('.node') length === 3 and every one of their id attributes matches /flowchart-[A-Za-z0-9_-]+-\d+$/ (the pipeline's regex at svgPostProcessing.ts:526)
      4. every '.edgePaths path' marker-end attribute's url(#ID) reference resolves to an existing marker element in the document (record the observed marker id substrings in the README — flowchart uses the point-start/point-end marker family under v11; assert resolution, and assert whatever id-family substring was actually observed)
      5. a '.root' element exists whose children's class lists cover 'nodes', 'edgePaths' (or 'edges'), and 'edgeLabels' — the three groups the pipeline's reorder at svgPostProcessing.ts:411-420 depends on; record the observed child order in the README but do NOT assert order (the pipeline exists to reorder)
      6. rect.background probe: the pipeline's edge-label fallback chain starts at rect.background (svgPostProcessing.ts:355). Determine empirically whether this fixture emits any. If yes, assert the observed count. If no, note it and carry the probe to Task 2's sequence fixture (or a minimal flowchart variant, e.g. styled edge labels) — the family must be either captured with a real observed value or documented in the README as not-emitted-under-v11 with the finding flagged for Phase 22. Never write an assertion whose expected value was not observed.
    - Suite-level rules: describe block timeout 30000 (renders cost seconds each); no whole-SVG snapshot mechanism of any kind; no assertions on path data, translate coordinates, or style values (coordinates legitimately shift in v12 — Phase 22 re-baselines those); a 0-match on any recorded selector is a failure, by construction of the exact counts above.
    - Commit only the two files this task names.

    Create tests/goldens/README.md documenting: the capture command (pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts), the installed-version check command, capture date and mermaid version (11.17.2), the 0-match-equals-failure rule, the never-weaken rule (Phase 22 owns pipeline selector updates if v12 legitimately drifts), and an observed-selector table (selector | fixture | observed value under v11) seeded with what this capture observed.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts (exit 0); git status --short shows only the two named files staged for the commit; node -p "require('./node_modules/mermaid/package.json').version" printed 11.17.2 before the run</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/mermaid/__tests__/structure-goldens.test.ts exists and contains at least one describe block with timeout 30000 rendering FLOWCHART through renderDiagram
    - pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts exits 0 on mermaid 11.17.2
    - The test file asserts .edgePaths path.flowchart-link count equal to 3 (exact number present in the source)
    - The test file contains the regex /flowchart-[A-Za-z0-9_-]+-\d+$/ applied to .node id attributes
    - The test file asserts every .edgePaths path marker-end reference resolves to an existing marker id
    - git grep -n "Snapshot" -- src/lib/mermaid/__tests__/structure-goldens.test.ts returns no matches (structural assertions only, no snapshot mechanism)
    - tests/goldens/README.md exists, names mermaid 11.17.2 and 2026-09-12, and contains the observed-selector table
    - The commit diff contains only structure-goldens.test.ts and tests/goldens/README.md
  </acceptance_criteria>
  <done>The v11 flowchart structural contract is committed as an executable golden suite, green under mermaid 11.17.2, with capture provenance recorded.</done>
</task>

<task type="auto">
  <name>Expand golden baseline — sequence-diagram contract</name>
  <files>src/lib/mermaid/__tests__/structure-goldens.test.ts, tests/goldens/README.md</files>
  <read_first>
    - src/utils/__tests__/postProcessDiagramSvg.test.ts lines 60-90 — the existing sequence-diagram fixture shape (messageLine neutralization test)
    - src/utils/svgPostProcessing.ts lines 455-470 — the pipeline queries '.edgePaths path, .messageLine0, .messageLine1, .messageLine' and syncs their marker-end colors
    - src/lib/mermaid/__tests__/structure-goldens.test.ts — the file this task extends (keep style consistent)
  </read_first>
  <action>
    Extend the golden suite with fixture SEQUENCE (exact source):
      sequenceDiagram
        participant A as Client
        participant B as Server
        A->>B: Request
        B-->>A: Response
    Render via renderDiagram(SEQUENCE, 'golden_seq') with the same getBBox-error tolerance. Assert, recording observed values:
    1. exactly one path with class messageLine0 and one with class messageLine1 exist (the solid and the dashed message — the pipeline color-sync loop at svgPostProcessing.ts:460 iterates these classes)
    2. every messageLine path's marker-end url(#ID) resolves to an existing marker element; record the observed marker id substrings in the README (v11 sequence diagrams use an arrowhead-family id — assert the substring family actually observed)
    3. two .messageText elements exist (one per message)
    4. if Task 1 could not capture rect.background, probe it here (sequence diagram output) or with one additional minimal fixture; capture it with a real observed value or document the not-emitted finding in the README as flagged for Phase 22
    Extend the README observed-selector table with the sequence rows. Commit only the two named files.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts (exit 0, now covering both fixtures)</automated>
  </verify>
  <acceptance_criteria>
    - The test file contains the SEQUENCE fixture source with participant A as Client, participant B as Server, one ->> and one -->> message
    - The suite asserts messageLine0 count 1 and messageLine1 count 1, both with resolvable marker-end references
    - pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts exits 0 with both describe groups present
    - tests/goldens/README.md observed-selector table includes the messageLine and sequence marker rows
    - The commit diff contains only the two named files
  </acceptance_criteria>
  <done>PIPE-01 capture is complete: both fixture families' v11 structural contracts are committed, green, and provenance-documented — still with mermaid 11.17.2 installed.</done>
</task>

<task type="auto">
  <name>Pin v11 defaults in doInit() and prove no-op under v11</name>
  <files>src/lib/mermaid/core.ts</files>
  <read_first>
    - src/lib/mermaid/core.ts lines 20-48 — doInit() and the existing mermaid.initialize object
    - src/constants/themeDerivation.ts lines 900-940 — where the style panel writes config.layout into per-diagram frontmatter (why the pin must live in initialize() only: mermaid config precedence is frontmatter > initialize() > per-type default > global default)
    - .planning/phases/21-upgrade-compatibility/21-RESEARCH.md — UPG-02 row and the verified core.ts edit (official mermaid recipe)
    - .planning/phases/21-upgrade-compatibility/21-CONTEXT.md — locked decision bullets
  </read_first>
  <action>
    In doInit(), add exactly two new keys to the existing mermaid.initialize object, immediately after the theme key: layout: 'dagre' and look: 'classic'. Change nothing else in the file — no reordering, no reformatting, no additional config. The pins neutralize mermaid 12's two silent default flips (ELK layout for 7 diagram types; redux-color/neo look for 10 types) and are no-ops under v11, where dagre/classic ARE the defaults. Per the locked decision, the pins live ONLY in the initialize() object — the per-diagram frontmatter config.layout written by the style panel must keep outranking them (that precedence is mermaid-native and must not be worked around in app code).
    Then run the empirical harmlessness gate (research Pitfall 7 / assumption A2): the goldens and the existing rendered suites must stay green on v11 WITH the pins applied. If anything changes under v11, STOP — the pins are not no-ops in this environment and the plan's sequencing premise is broken; surface before proceeding.
    Commit only src/lib/mermaid/core.ts.
  </action>
  <verify>
    <automated>pnpm run type-check (exit 0); pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts src/utils/__tests__/postProcessDiagramSvg.test.ts src/lib/mermaid/__tests__/subgraph-render.test.ts (exit 0 on mermaid 11.17.2)</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/mermaid/core.ts doInit() initialize object contains the keys layout: 'dagre' and look: 'classic', adjacent to the existing theme key
    - pnpm run type-check exits 0
    - The golden suite, the pipeline test suite, and the subgraph render suite all exit 0 with the pins in place and mermaid 11.17.2 still installed
    - git diff of the commit touches only src/lib/mermaid/core.ts and adds exactly the two config keys
  </acceptance_criteria>
  <done>UPG-02 landed and empirically proven harmless under v11: rendering behavior identical (goldens green), user frontmatter overrides untouched, revert-safe position for the flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mermaid output → DOM | Rendered SVG crosses into the app DOM; sanitization (DOMPurify, core.ts render path) is NOT modified by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | SVG sanitization path | — | accept | Out of scope this plan: DOMPurify stays in the render path untouched (core.ts renderDiagram); the known foreignObject edge case remains tracked in .planning/codebase/CONCERNS.md |
| T-21-03 | DoS | Renders on 2-day-old v12.0.0 (upcoming flip) | medium | mitigate | This plan builds the mitigation itself: the golden suite catches structural render breakage loudly at the flip; exact pin + revert path documented in Plan 02 |
</threat_model>

<verification>
- pnpm exec vitest run src/lib/mermaid/__tests__/structure-goldens.test.ts — green, covering flowchart + sequence structural contracts
- pnpm exec vitest run src/utils/__tests__/postProcessDiagramSvg.test.ts src/lib/mermaid/__tests__/subgraph-render.test.ts — still green with the pins applied (targeted runs only per the VALIDATION.md sampling rate; full suite runs at the Plan 03 gate)
- pnpm run type-check — green
- git log for this plan shows three commits, each containing only its named files; node_modules/mermaid still reports 11.17.2
</verification>

<success_criteria>
- PIPE-01 capture half: committed golden suite encodes the v11 structural contract (exact counts, id-pattern regexes, 1:1 correlations, marker resolution, .root groups) while v11 is installed — the pre-flip baseline exists
- UPG-02: layout/look pins landed in doInit() and proven no-op under v11 by the golden + pipeline suites
- Capture provenance (README) lets Phase 22 audit exactly what was observed and when
</success_criteria>

<output>
Create `.planning/phases/21-upgrade-compatibility/21-01-SUMMARY.md` when done
</output>

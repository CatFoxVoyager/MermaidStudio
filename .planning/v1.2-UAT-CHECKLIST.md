# v1.2 Mobile UI - User Acceptance Testing Checklist

**Version:** 0.6.0 (unreleased)
**Device Targets:** Real mobile device (smartphone) + desktop browser ≥1280px
**Completion Goal:** 100% of items checked before version bump
**Purpose:** This checklist gates the 0.6.0 release. Every mobile flow from v1.1 (Phases 14-18) + v1.2 (Phase 19) must be validated on real hardware before the version bump is applied.

> **⚠️ CRITICAL:** Do NOT bump the version or create the v0.6.0 tag until ALL items below are checked. The current version is STILL 0.5.1 — see the "Release (100% Required)" section at the end for the final post-validation steps.

## Foundation / Viewport Detection

### Viewport Breakpoints (375/768 Boundary)
- [ ] **Mobile layout at 375px:** Open app on mobile device (or browser DevTools mobile emulation) — at 375px viewport width, the dedicated mobile layout renders (MobileTopBar + MobileBottomNav visible, desktop shell hidden)
- [ ] **Mobile layout at 767px:** Resize viewport to 767px — the mobile layout still renders (no hybrid/broken intermediate state)
- [ ] **Desktop layout at 768px:** Resize viewport to 768px — the desktop layout returns (desktop TopBar + Sidebar visible, mobile shell hidden)
- [ ] **No horizontal overflow at 375px:** Scroll horizontally at 375px — no horizontal overflow appears (content fits viewport)

### Viewport Meta Tag & Safe Area
- [ ] **Viewport meta tag present:** Inspect page source — `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` is present
- [ ] **Safe-area insets on notched device:** Open app on a notched device (e.g., iPhone X+) — content is NOT obscured by notch/speaker/home indicator (env() safe-area respected)
- [ ] **Dynamic viewport units work:** Open app on mobile browser — when browser chrome appears/disappears, app fills visible area correctly (dvh units handle dynamic viewport)

## Mobile Shell

### Mobile TopBar (375px Width)
- [ ] **MobileTopBar visible at 375px:** View app at 375px — MobileTopBar is visible at top (brand + primary actions + overflow menu trigger)
- [ ] **Brand fits without truncation:** Check MobileTopBar — brand text/logo is fully visible (not truncated with "...") at 375px width
- [ ] **TopBar overflow menu opens/closes:** Tap overflow menu trigger (≡ or kebab icon) — menu opens, tap backdrop/menu item — menu closes
- [ ] **Overflow menu contains correct actions:** Open overflow menu — it shows New/Save + other primary actions (same as desktop TopBar actions)

### Bottom Navigation (Files / Edit / AI)
- [ ] **Bottom navigation visible:** MobileBottomNav is visible at bottom with 3 destinations: Files / Edit / AI
- [ ] **Thumb-reachable placement:** Bottom nav is positioned at bottom of screen (thumb-zone reachable, not at top)
- [ ] **Active destination highlighted:** Tap each destination — the active destination has a visual indicator (highlight/color change)
- [ ] **Tapping destination switches view:** Tap "Files" → File drawer opens; tap "Edit" → Editor view; tap "AI" → AI drawer opens

### Desktop Non-Regression (Shell)
- [ ] **Desktop shell hidden at 375px:** View app at 375px — desktop TopBar + Sidebar are NOT visible
- [ ] **Desktop shell visible at 1280×800:** View app at 1280×800 — desktop TopBar + Sidebar are visible and functional (mobile shell hidden)

## Workspace

### Code/Preview Toggle (Mobile ≤600px)
- [ ] **Code/Preview toggle visible below 600px:** View app at mobile width — a segmented Code↔Preview toggle is visible below the TopBar
- [ ] **Code tab shows CodeMirror:** Tap "Code" — CodeMirror editor is visible in the main workspace area
- [ ] **Preview tab shows diagram:** Tap "Preview" — Mermaid diagram preview is visible in the workspace
- [ ] **Toggle preserves scroll position:** Enter text in Code tab, scroll to line 10, tap "Preview", tap "Code" — scroll position is preserved (still at line 10)

### Visual Tab (Phase 19 Wiring)
- [ ] **Visual tab accessible in mobile workspace:** The 3-segment toggle includes a "Visual" option (or Visual entry is reachable via mobile layout)
- [ ] **Visual tab shows visual editor:** Tap "Visual" — VisualEditorCanvas is mounted and visible in the workspace

### Desktop Non-Regression (Workspace)
- [ ] **Desktop split-view visible at 1280×800:** View app at 1280×800 — side-by-side Code editor + Preview are visible (no segmented toggle)

## Drawers (Files / AI / Colors / Advanced)

### Files Drawer (Sidebar)
- [ ] **Files drawer opens from bottom-nav "Files":** Tap bottom-nav "Files" — Sidebar opens as a slide-over drawer from the right
- [ ] **Files drawer closes via backdrop tap:** With Files drawer open, tap the dark backdrop — drawer closes and returns to prior view

### AI Drawer
- [ ] **AI drawer opens from "AI":** Tap bottom-nav "AI" — AI assistant panel opens as a drawer with full chat functionality
- [ ] **AI drawer chat works:** Send a message in the AI drawer — response appears (desktop-equivalent AI features work)

### Colors & Advanced Drawers (Mutual Exclusion)
- [ ] **Colors drawer opens:** From the editor, open the DiagramColors panel — it opens as a slide-over drawer on mobile
- [ ] **Advanced drawer opens:** Open the AdvancedStyle panel — it opens as a slide-over drawer
- [ ] **Mutual exclusion works:** Open Files drawer, then tap Colors — Files drawer closes, Colors drawer opens (only one drawer at a time)
- [ ] **All drawers render as slide-over:** All drawers (Files/AI/Colors/Advanced) render with position="right" slide-over behavior on mobile

## Visual Editor Touch Interactions (Phase 18 + 19)

### Touch Selection
- [ ] **Node selection via touch tap:** In the Visual tab, tap a node — it becomes selected (highlighted)
- [ ] **Multiple-node touch selection:** Touch-select multiple nodes — all selected nodes are highlighted

### Touch Drag & Drop
- [ ] **Touch drag moves nodes:** Touch-drag a selected node — it moves on the canvas following touch position
- [ ] **Drag syncs to code:** After dragging a node to a new position, check the Code tab — the Mermaid code reflects the new position (VIS-03 sync)

### Pinch-to-Zoom & Pan
- [ ] **Pinch-to-zoom works on canvas:** Use two-finger pinch gesture on the visual editor — canvas zooms in/out smoothly
- [ ] **Pan after zoom works:** After zooming, use one finger to pan — canvas moves within the zoomed viewport

### Desktop Non-Regression (Visual Editor)
- [ ] **Desktop mouse selection still works at 1280×800:** On desktop, click-drag to select nodes — mouse-based selection still functions (no regression from Phase 18 pointer-events migration)

## Touch Interactions (General)

### Tap Target Size
- [ ] **All tap targets ≥44×44px (measured):** Using browser DevTools measurement, verify primary buttons (BottomNav, TopBar overflow, drawer close buttons) have ≥44×44px tappable area
- [ ] **Adequate spacing between targets:** Tap targets have adequate spacing — no mis-taps when tapping adjacent controls

### Touch Scroll & Active States
- [ ] **Diagram preview scrolls with touch pan:** In the Preview tab, use one finger to pan — diagram scrolls smoothly
- [ ] **Active states on button tap:** Tap any mobile button — visual feedback (active:bg-white/15 or similar) appears during tap
- [ ] **No hover-only interactions:** Hover over any interactive element — all interactions are tap/active-based (no hover-only behavior that would fail on touch)

## Desktop Non-Regression (Comprehensive)

### Desktop Layout & Controls
- [ ] **App renders desktop layout at 1280×800:** View app at 1280×800 — desktop layout is pixel-perfect (no mobile artifacts)
- [ ] **All desktop controls visible:** All desktop controls are visible and functional (TopBar, Sidebar, modals, style panels)
- [ ] **No mobile layout artifacts:** No MobileTopBar, MobileBottomNav, or mobile drawer behavior appears at desktop viewport
- [ ] **Desktop visual editor mouse selection works:** Click-drag to select nodes in the visual editor — mouse selection works (no regression from touch migration)

## HTTPS / Self-Signed Certificate (Real-Browser)

### Manual Cert Acceptance (Dev Server)
- [ ] **Dev server HTTPS works:** Run `npm run dev` — server starts on port 5173 with HTTPS (`.cert/cert.pem` is loaded)
- [ ] **Browser prompts for cert acceptance:** Open `https://localhost:5173` in a real browser — browser shows security warning (self-signed cert)
- [ ] **Manual cert acceptance works:** Accept the self-signed cert in the browser — app loads and runs normally
- [ ] **Dev server stays responsive after cert acceptance:** After accepting cert, all app features work (editor, preview, visual editor, AI)

> **Note:** This is the manual step that automated E2E (Playwright) cannot perform due to the self-signed cert blocker documented in Phase 20-01. Real-browser validation covers this gap.

## Release (100% Required) — DEFERRED Manual Step

> **⚠️ CRITICAL:** This section is PERFORMED BY THE USER AFTER 100% VALIDATION — NOT in Phase 20. The version is STILL 0.5.1. Only after all UAT items above are checked should you proceed with these steps.

### Pre-Release Checks
- [ ] **All E2E tests passing:** Run `npm run test:e2e` — all mobile E2E specs (mobile-detection, touch-targets, touch-interactions, visual-editor-touch) pass
- [ ] **All UAT items above checked:** Every checkbox in the sections above is checked (100% validation complete)
- [ ] **CHANGELOG.md reviewed and accurate:** Review `CHANGELOG.md` — the 0.6.0 (unreleased) section accurately summarizes v1.1 + v1.2 mobile work

### Version Bump (0.5.1 → 0.6.0)
- [ ] **Version bumped to 0.6.0:** Run `npm version 0.6.0` (or manually edit `package.json` → `"version": "0.6.0"`)
- [ ] **CHANGELOG.md date updated:** Update `CHANGELOG.md` — change `## [0.6.0] - unreleased` to `## [0.6.0] - 2026-07-XX` (today's date)
- [ ] **Git commit created:** Commit the changes: `git commit -m "chore(release): bump version to 0.6.0 for mobile UI integration"`

### Git Tag & Push
- [ ] **Git tag v0.6.0 created:** Run `git tag v0.6.0` — the annotated tag is created at the current commit
- [ ] **Tag pushed to remote:** Run `git push origin v0.6.0` — the tag is pushed to GitHub (triggers CI/CD if configured)
- [ ] **Version confirmed in published app:** After deployment, verify the app shows version 0.6.0 (e.g., in package.json, About modal, or UI)

### Deployment (Optional)
- [ ] **Vercel deployment triggered (if applicable):** Pushing the tag triggers Vercel deployment — verify at the production URL
- [ ] **Production works on mobile devices:** Test the deployed app on a real mobile device — all UAT items still pass in production

---

**Validation Complete:** When all Release section items are checked, the 0.6.0 release is live and the mobile UI integration (v1.1 + v1.2) is shipped to users.

**Reference:** This checklist implements QA-01 (viable E2E + manual UAT) and REL-01 (0.6.0 release preparation) from Phase 20. Per CONTEXT Zone 4, the version bump is explicitly gated to post-validation to ensure "0.6.0 quand tout sera 100% ok" (0.6.0 when everything is 100% OK).
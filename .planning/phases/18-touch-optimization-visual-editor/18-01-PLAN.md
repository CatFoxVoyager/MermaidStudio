---
phase: 18-touch-optimization-visual-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/mobile/MobileTopBar.tsx
  - src/components/mobile/MobileBottomNav.tsx
  - src/components/preview/PreviewPanel.tsx
  - tests/e2e/tests/mobile-touch/touch-targets.spec.ts
  - tests/e2e/tests/mobile-touch/touch-interactions.spec.ts
autonomous: true
requirements: [MTCH-01, MTCH-02]
user_setup: []

must_haves:
  truths:
    - "Every interactive control rendered in the mobile shell (≤768px) has a tappable area of at least 44×44px (MTCH-01, per Zone 1 decision)"
    - "No interactive control on mobile relies on a :hover-only affordance; each hover state has an active/tap equivalent (MTCH-02, per Zone 2 decision)"
    - "The diagram preview container enables native one-finger touch scroll/pan via CSS touch-action (MTCH-02, per Zone 2 decision)"
    - "Desktop (≥768px) controls and preview behavior are unchanged — no className regression outside max-md: scope"
  artifacts:
    - path: "src/components/mobile/MobileTopBar.tsx"
      provides: "MobileTopBar buttons padded to ≥44×44px with active: tap states"
      contains: "min-w-[44px] min-h-[44px]"
    - path: "src/components/mobile/MobileBottomNav.tsx"
      provides: "Bottom-nav buttons verified/raised to ≥44px with active: tap state"
    - path: "src/components/preview/PreviewPanel.tsx"
      provides: "Preview scroll container with touch-action enabling native touch pan"
      contains: "touchAction"
    - path: "tests/e2e/tests/mobile-touch/touch-targets.spec.ts"
      provides: "MTCH-01 E2E: ≥44px tap-target audit at 375px"
    - path: "tests/e2e/tests/mobile-touch/touch-interactions.spec.ts"
      provides: "MTCH-02 E2E: preview touch-action present + active: states"
  key_links:
    - from: "tests/e2e/tests/mobile-touch/touch-targets.spec.ts"
      to: "src/components/mobile/MobileTopBar.tsx"
      via: "Playwright boundingBox assertion at 375px viewport asserts width>=44 and height>=44 on each topbar button"
      pattern: "boundingBox"
    - from: "src/components/preview/PreviewPanel.tsx"
      to: "native browser touch scroll"
      via: "inline style touchAction on the preview-grid scroll container"
      pattern: "touchAction.*pan-x pan-y"
---

<objective>
Deliver MTCH-01 (≥44px tap targets) and MTCH-02 (no hover-only interactions + native touch scroll/pan) for the mobile shell and diagram preview. Targeted className adjustments only — no global refactor, no new dependencies.

Purpose: Phases 14-17 built the mobile shell; this plan closes the touch ergonomics gap so every mobile control is comfortably tappable and the preview scrolls with one finger. These are the two lowest-risk requirements in Phase 18 and share the same mobile-shell files, so they ship together ahead of the MTCH-03 keystone (plan 18-02).

Output: MobileTopBar/MobileBottomNav buttons raised to ≥44px with active: states; PreviewPanel gains CSS touch-action for native pan; two Playwright E2E specs (Wave-0 test scaffolds created inline) prove the behavior at 375px.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-touch-optimization-visual-editor/18-CONTEXT.md
@.planning/phases/18-touch-optimization-visual-editor/18-RESEARCH.md

@src/components/mobile/MobileTopBar.tsx
@src/components/mobile/MobileBottomNav.tsx
@src/components/preview/PreviewPanel.tsx
@src/components/layout/MobileWorkspace.tsx
@tests/e2e/tests/mobile-foundation/mobile-detection.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise mobile-shell tap targets to ≥44px and add active: states (MTCH-01 + MTCH-02 hover-replacement half)</name>
  <files>src/components/mobile/MobileTopBar.tsx, src/components/mobile/MobileBottomNav.tsx</files>
  <read_first>
    - src/components/mobile/MobileTopBar.tsx (full — 81 lines; note the three buttons at lines 46-76 with className "p-2 rounded-lg transition-colors hover:bg-white/8" → icon 18px + p-2(8px) = 34px, BELOW 44px)
    - src/components/mobile/MobileBottomNav.tsx (full — 52 lines; nav is h-[56px] with icon 20px + label → each slot already ≥44px tall, but lacks an active: state)
    - src/components/shared/Modal.tsx lines 94-101 (reference pattern: "max-md:p-2 min-w-[44px] min-h-[44px]" — reuse the SAME tailwind tokens for consistency with Phase 17 work)
    - 18-RESEARCH.md "Code Examples → Enforce ≥44px Tap Targets" and "Replace Hover-Only States with Active States"
  </read_first>
  <action>
For each of the three buttons in MobileTopBar.tsx (New diagram `mobile-topbar-new`, Save `mobile-topbar-save`, Overflow `mobile-topbar-overflow`):
- Replace the className "p-2 rounded-lg transition-colors hover:bg-white/8" with "p-3 rounded-lg transition-colors hover:bg-white/8 active:bg-white/15 min-w-[44px] min-h-[44px] flex items-center justify-center" (per Zone 1 decision — targeted ≥44px fixes, not a global refactor). The `min-w-[44px] min-h-[44px]` tokens match the Modal close-button convention from Phase 17; `active:bg-white/15` is the MTCH-02 tap-equivalent for the existing hover-only affordance (per Zone 2 decision — replace hover-only with tap/active). Keep `hover:bg-white/8` so desktop hover still works (Modal close button keeps hover too — do NOT remove hover, ADD active).
- Leave the `style={{ color: 'var(--text-secondary)' }}`, `aria-label`, `title`, `data-testid`, `onClick`, and icon size (18) unchanged.

For MobileBottomNav.tsx, each of the three nav buttons (`mobile-nav-files`, `mobile-nav-edit`, `mobile-nav-ai`) already meets ≥44px height (h-[56px] row, icon+label) — do NOT change sizing. Only add an `active:bg-white/10` tap-equivalent to the className (append to the existing template string after `transition-colors`) so the touch-down state is no longer hover-only. Leave the active-view border-top accent logic intact.

Do not touch any other file. Do not change desktop (≥768px) components — these className additions apply uniformly but only the mobile shell renders these components.
  </action>
  <verify>
    <automated>npm test -- --run src/components/mobile 2>&1 | tail -20</automated>
  </verify>
  <done>
- All three MobileTopBar buttons have class tokens "min-w-[44px]", "min-h-[44px]", and "active:bg-white/15".
- All three MobileBottomNav buttons have "active:bg-white/10" in their className and their height/sizing is unchanged.
- `npm run type-check` passes; existing mobile component unit tests still pass.
- No hover token was removed (additive change only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preview touch-action (MTCH-02 pan half) + Wave-0 E2E scaffolds for MTCH-01 and MTCH-02</name>
  <files>src/components/preview/PreviewPanel.tsx, tests/e2e/tests/mobile-touch/touch-targets.spec.ts, tests/e2e/tests/mobile-touch/touch-interactions.spec.ts</files>
  <behavior>
    - MTCH-01: At 375×812 viewport, every interactive button in the mobile top bar and bottom nav has a boundingBox whose width AND height are each >= 44.
    - MTCH-02: At 375×812, the preview scroll container exposes touch-action containing "pan-x" and "pan-y" (computed style or inline style) so native one-finger pan is enabled.
    - MTCH-02: The mobile top bar buttons expose an active: tap-equivalent class token (active:bg-white/15) — assertable via class membership on the rendered button.
    - Non-regression: At 1280×800 (desktop), the preview container still renders and the touch-action style does not break the layout (preview-panel testid visible).
  </behavior>
  <read_first>
    - src/components/preview/PreviewPanel.tsx lines 1262-1269 (the scroll container `<div ref={containerRef} className="flex-1 overflow-auto preview-grid ...">` — this is the MTCH-02 touch-action target; note it already has onMouseDown={handlePanMouseDown} for desktop drag-pan which must remain)
    - 18-RESEARCH.md "Code Examples → Apply CSS touch-action to Preview Panel" (lines 420-445) and "Open Questions → MTCH-02 Touch Interaction Conflicts" (the dynamic touch-action: none during isPanning recommendation)
    - tests/e2e/tests/mobile-foundation/mobile-detection.spec.ts (full — the established E2E pattern: page.goto('/'), setViewportSize, waitForLoadState + waitForTimeout(3000), getByTestId assertions — mirror this structure)
    - src/components/mobile/MobileTopBar.tsx data-testid values (mobile-topbar-new, mobile-topbar-save, mobile-topbar-overflow) and MobileBottomNav (mobile-nav-files, mobile-nav-edit, mobile-nav-ai) — use these in the boundingBox audit
  </read_first>
  <action>
**Source change (PreviewPanel.tsx, MTCH-02 pan):** On the preview scroll container div (the one with `ref={containerRef}` and className "flex-1 overflow-auto preview-grid ..."), add an inline `style` object that sets `touchAction` based on panning state so native touch pan is enabled by default but yields to the JS drag-pan while it is active:
`style={{ touchAction: isPanning ? 'none' : 'pan-x pan-y pinch-zoom' }}`
This resolves the RESEARCH open-question conflict: when `isPanning` is true (desktop drag-pan engaged via onMouseDown), touch-action none lets the JS scroll logic own the gesture; otherwise native one-finger pan and pinch-zoom are enabled. Merge with any existing inline style on the container (the cursor classes already live in className via `isPanning ? 'cursor-grabbing' : 'cursor-grab'`, so the inline style is additive). Do NOT remove onMouseDown/onDragOver/onDrop handlers — desktop drag-pan must keep working (this is the desktop non-regression guard). Do NOT add JS touchmove handlers — per Zone 2 decision, basic pan is CSS-only.

**E2E scaffolds (Wave 0 — these test files do not yet exist; create them):**

Create `tests/e2e/tests/mobile-touch/touch-targets.spec.ts` (MTCH-01): mirror the mobile-detection.spec.ts harness (page.goto('/'), setViewportSize to 375×812, waitForLoadState('domcontentloaded'), waitForTimeout(3000)). For each of the six testids (mobile-topbar-new, mobile-topbar-save, mobile-topbar-overflow, mobile-nav-files, mobile-nav-edit, mobile-nav-ai), use `const box = await page.getByTestId(id).boundingBox();` then `expect(box.width).toBeGreaterThanOrEqual(44);` and `expect(box.height).toBeGreaterThanOrEqual(44);`. (The mobile-topbar-new button only renders when onNewDiagram is provided — guard with a count check or skip if absent; the save and overflow buttons always render.) Add a desktop non-regression check at 1280×800: the desktop `new-diagram-button` testid is visible (proves the mobile className change did not bleed into desktop rendering).

Create `tests/e2e/tests/mobile-touch/touch-interactions.spec.ts` (MTCH-02): same 375×812 harness. Switch to the preview pane (click `mobile-workspace-tab-preview`), then assert the preview scroll container exposes native pan: locate the `.preview-grid` element inside `[data-testid="preview-panel"]`, and assert its computed style enables pan — `const ta = await page.locator('[data-testid="preview-panel"] .preview-grid').evaluate(el => getComputedStyle(el).touchAction);` then `expect(ta).toMatch(/pan-x/);` and `expect(ta).toMatch(/pan-y/);`. Also assert each mobile-topbar button class list contains "active:bg-white/15" via `await expect(page.getByTestId('mobile-topbar-save')).toHaveClass(/active:bg-white\/15/);`. Add a desktop non-regression check: at 1280×800 the preview-panel testid is visible.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/tests/mobile-touch/touch-targets.spec.ts tests/e2e/tests/mobile-touch/touch-interactions.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <done>
- PreviewPanel.tsx preview-grid container has inline `touchAction` set to 'pan-x pan-y pinch-zoom' (default) and 'none' (when isPanning).
- Desktop onMouseDown drag-pan handlers remain intact (no desktop regression).
- Both E2E specs exist and pass on chromium at 375×812: all six mobile controls have boundingBox >= 44 in both dimensions; the preview-grid computed touch-action matches pan-x and pan-y; mobile top bar buttons carry the active: token.
- Desktop non-regression assertions at 1280×800 pass (preview-panel visible, desktop new-diagram-button visible).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser-touch-runtime | Untrusted touch input crosses from mobile browser into the React handler layer; this plan only changes CSS className tokens and inline touch-action — no new JS input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | CSS className tokens (min-w/min-h, active:) | low | accept | Pure presentation tokens; no data flow. Acceptable — no security-relevant state. |
| T-18-02 | Information Disclosure | PreviewPanel touch-action inline style | low | accept | touch-action is a presentation hint; it cannot read user data. Acceptable. |
| T-18-03 | Denial of Service | E2E specs (Wave 0 creation) | low | accept | Test-only artifacts; cannot affect production runtime. Acceptable. |
</threat_model>

<verification>
- `npm run type-check` clean (no TS regressions from className edits).
- `npm test -- --run src/components/mobile src/components/preview` green (existing unit tests).
- `npx playwright test tests/e2e/tests/mobile-touch/ --project=chromium` green (new MTCH-01/02 specs).
- `npm run lint` clean (eslint max-warnings 999 not exceeded).
- Visual spot-check (optional, not blocking): dev server at 375px — top bar buttons visibly larger; preview pans with one-finger drag in the preview pane.
</verification>

<success_criteria>
- MTCH-01: every mobile-shell interactive control audited has boundingBox width>=44 and height>=44 at 375px (E2E proof).
- MTCH-02: preview container computed touch-action matches pan-x AND pan-y (native one-finger pan enabled); no mobile control remains hover-only (active: token present).
- Desktop (>=768px) preview and controls behaviorally unchanged (desktop E2E assertions pass).
- Zero new dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/18-touch-optimization-visual-editor/18-01-SUMMARY.md` when done. Note in the summary: which controls were raised to 44px, the touch-action conditional logic, and confirmation that desktop drag-pan handlers are intact — plan 18-02 (MTCH-03 keystone) relies on PreviewPanel.tsx NOT being regressed but does NOT modify it, so 18-01 and 18-02 have zero file overlap.
</output>

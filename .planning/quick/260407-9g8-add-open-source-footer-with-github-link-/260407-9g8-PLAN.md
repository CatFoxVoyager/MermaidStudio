---
phase: 260407-9g8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/StatusBar.tsx
autonomous: true
requirements:
  - QUICK-260407-9g8

must_haves:
  truths:
    - "User sees 'MermaidStudio is open source' text in the footer at all times (regardless of tab state)"
    - "User sees a GitHub icon (octocat SVG) next to the open source text"
    - "Clicking the link opens https://github.com/CatFoxVoyager/MermaidStudio in a new tab"
    - "Open source notice sits on the RIGHT side of the existing status bar, after render time and saved time"
    - "No new footer row is created"
  artifacts:
    - path: "src/components/editor/StatusBar.tsx"
      provides: "Status bar with integrated GitHub link"
      contains: "MermaidStudio is open source"
  key_links:
    - from: "StatusBar.tsx anchor element"
      to: "https://github.com/CatFoxVoyager/MermaidStudio"
      via: "href attribute"
      pattern: "href.*github\.com/CatFoxVoyager/MermaidStudio"
---

<objective>
Add an open source notice with GitHub repository link to the existing StatusBar component at the bottom of the MermaidStudio app.

Purpose: Makes the project's open source nature visible and provides a direct link to the repository for contributions.
Output: Modified StatusBar.tsx with integrated GitHub link on the right side.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260407-9g8-add-open-source-footer-with-github-link-/260407-9g8-CONTEXT.md
@.planning/quick/260407-9g8-add-open-source-footer-with-github-link-/260407-9g8-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add open source GitHub link to StatusBar right side</name>
  <files>src/components/editor/StatusBar.tsx</files>
  <action>
Modify `src/components/editor/StatusBar.tsx` to add a GitHub link to the right side of the existing status bar. This implements all CONTEXT.md decisions: integrate into existing bar (not a new row), GitHub icon + "View on GitHub" text, URL https://github.com/CatFoxVoyager/MermaidStudio, display "MermaidStudio is open source", right-side placement.

Implementation steps:

1. Add a `GitHubIcon` inline SVG component at the top of the file (before `StatusBar`). Use this exact SVG path for the GitHub octocat:
```tsx
function GitHubIcon({ size = 12 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
    </svg>
  );
}
```

2. In the right-side `<div className="flex items-center gap-3">` (line 48), after the `{relSaved()}` span, add a visual separator and the GitHub link. The link must be STATIC -- it renders regardless of tab state (not inside any conditional). Add these elements after the `<span>{relSaved()}</span>` on line 50:

```tsx
<span className="mx-1 opacity-30">|</span>
<a
  href="https://github.com/CatFoxVoyager/MermaidStudio"
  target="_blank"
  rel="noopener noreferrer"
  className="flex items-center gap-1 hover:opacity-80 transition-opacity"
>
  <GitHubIcon size={12} />
  <span>MermaidStudio is open source</span>
</a>
```

IMPORTANT:
- Do NOT use `Github` from lucide-react -- it is deprecated and not exported in v1.7.0. Use the inline SVG `GitHubIcon` component instead.
- Do NOT create a new footer row or component. Modify the existing StatusBar only.
- The link inherits `var(--text-tertiary)` color and `text-[10px]` size from the parent div -- no additional color styling needed.
- `target="_blank" rel="noopener noreferrer"` is required for external links (security: prevents tab-napping).
- The separator `|` with `opacity-30` provides visual separation between status info and the open source notice.
  </action>
  <verify>
    <automated>cd D:/code/MermaidStudio && npm run type-check 2>&1 | tail -5</automated>
    Manual verification: open the app, check the footer bar right side shows GitHub icon + "MermaidStudio is open source", click the link to confirm it opens https://github.com/CatFoxVoyager/MermaidStudio in a new tab.
  </verify>
  <done>
    StatusBar.tsx contains GitHubIcon inline SVG component and an anchor element linking to https://github.com/CatFoxVoyager/MermaidStudio. The link appears on the right side of the existing status bar, after the render time and saved time indicators, separated by a `|` divider. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with no errors
2. `npm run build` succeeds (no import or bundling issues)
3. Footer bar shows "MermaidStudio is open source" with GitHub icon on the right side
4. Clicking the link opens https://github.com/CatFoxVoyager/MermaidStudio in a new browser tab
5. No new footer row created -- only the existing StatusBar is modified
</verification>

<success_criteria>
- StatusBar.tsx modified with GitHubIcon component and anchor link
- "MermaidStudio is open source" visible in footer at all times
- Link opens https://github.com/CatFoxVoyager/MermaidStudio in new tab
- TypeScript compiles, build succeeds
- No new footer row, no new components, no new files
</success_criteria>

<output>
After completion, create `.planning/quick/260407-9g8-add-open-source-footer-with-github-link-/260407-9g8-SUMMARY.md`
</output>

pnpm run lint
pnpm run type-check
# Full unit suite is run in CI (ci.yml `test:coverage` job). It is intentionally
# NOT run here: vitest's fork pool hangs at teardown in this project (worker
# crash), which made the hook unable to complete. Keep the hook fast (~10s).
